creation_rules:
  - path_regex: apps/production/.*\.sops\.yaml$
    encrypted_regex: ^(data|stringData)$
    # Replace with the public key printed by the sops Ansible task on first provisioning:
    #   ansible-playbook provisioning.yaml --tags flux-cd
    age: age1REPLACEME
