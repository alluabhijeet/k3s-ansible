# Encrypt before committing:
#   sops --encrypt --in-place apps/production/terminus/secret.sops.yaml
#
# Generate APP_SECRET with: openssl rand -hex 32
apiVersion: v1
kind: Secret
metadata:
  name: terminus-secrets
  namespace: terminus
type: Opaque
stringData:
  APP_SECRET: CHANGE_ME
